# Test artifacts

Generated full-pipeline outputs go here (for example `tauheed-primer_roman_urdu/`). Everything in this directory except this file and `.gitkeep` is ignored by git. Run baseline commands from inside this directory.
